//// Creating and using Arrays
//#include "stdio.h"
//
//int main () {
//
//    long numbers[10];   // The number inside the array is the size of the array
//
//    numbers[0] = 1; // The first element in the array has index 0
//
//    printf("%ld\n", numbers[0]);  // Prints the first array element
//
//    // Simple program to calculate grade average
//    int grades[10];
//    int sum = 0;
//    float average;
//
//    for (int i = 0; i < 10; i++) {
//        printf("Enter grade %d: ", i + 1);
//        scanf("%d", &grades[i]);
//        sum += grades[i];
//    }
//
//    average = sum / 10.0f;
//
//    printf("Sum of grades: %d\n", sum);
//    printf("Grade average: %0.2f", average);
//
//    return 0;
//
//    int arr[5] = {0, 0, 0, 0, 0}; // Initializing an array with values
//
//    float data[10] = {[4] =4.5};    // You can initialize in any order by specifying an element to initialize
//}
//
