/*
// While and Do-While Loops
#include "stdio.h"

int main () {

    // While loops are pre-test loops
    // Counter controlled while loop
    int count = 1;
    while (count <= 5) {
        printf("%i\n", count);
        count++;
    }

    // Logic controlled While Loop
    int num;
    scanf("%d", &num);
    while (num != -1) {
        scanf("%d", &num);
    }

    // Do-while Loops are post-test loops.  It will always run at least once
    int i;
    do {
        scanf("%d", &i);
    } while (i != 20);

    // While loops are usually used for logic controlled situations, while for loops are for counter controlled.

    return 0;
}

*/
